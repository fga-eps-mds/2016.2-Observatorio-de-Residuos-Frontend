language: node_js

rvm:
  - "4.5.0"

install:
 - npm install -g karma --save-dev

before_script:
 - npm install -g karma-jasmine
 - npm install -g phantomjs --save-dev
 - npm install -g karma-phantomjs-launcher --save-dev
 - npm install -g karma-coverage --save-dev

script:
  - karma start --single-run --browsers PhantomJS

after_success:
  - npm install codeclimate-test-reporter
  - CODECLIMATE_REPO_TOKEN=31c7a0fe383cb7ccb088534e21bb44fe5e84bbbf9b296fc4f0518da24cdcc26b codeclimate-test-reporter < coverage/PhantomJS\ 2.1.1\ \(Linux\ 0.0.0\)/lcov.info

notifications:
  email:
    recipients:
      - antoniojk7@gmail.com
      - leomeister22@gmail.com
      - lucas.advc@gmail.com
      - skingk.f@gmail.com
      - vitor.nere@hotmail.com

